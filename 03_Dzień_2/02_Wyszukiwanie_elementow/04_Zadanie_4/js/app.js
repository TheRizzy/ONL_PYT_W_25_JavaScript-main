const li = document.querySelectorAll("nav.navigation li");
console.log(li);

const paragrafs = document.querySelectorAll("div p");
console.log(paragrafs);

const tag_article = document.querySelectorAll("article div");
console.log(tag_article);

